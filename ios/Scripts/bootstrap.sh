#!/usr/bin/env bash
set -euo pipefail

echo "[bootstrap] Starting setup..."
# Add tool installation or setup steps here, e.g.:
# brew bundle --no-lock || true
# swift --version
echo "[bootstrap] Done."

